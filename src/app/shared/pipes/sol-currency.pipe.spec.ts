import { SolCurrencyPipe } from './sol-currency.pipe';

describe('SolCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new SolCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
